import React from "react";


const ListingDetails = ({ match }) => {

console.log(match);

    return (
        <div>
            <h1>Listing Details</h1>
        </div>
    );
};

export default ListingDetails;

